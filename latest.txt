version: '3'

services:
  webserver:
    image: nginx:latest
    ports:
      - 80:80
      - 443:443
    restart: always
    volumes:
      - ./default.conf:/etc/nginx/conf.d/default.conf
      - ./certbot/www:/var/www/certbot
      - ./certbot/conf/:/etc/nginx/ssl
  certbot:
    image: certbot/certbot:latest
    volumes:
      - ./creds.ini:/var/creds.ini
      - ./certbot/www:/var/www/certbot
      - ./certbot/conf:/etc/letsencrypt
    command: sh -c "apt -y update && apt -y install certbot && apt -y install python3-certbot-dns-digitalocean && certbot certonly --agree-tos --dns-digitalocean
--dns-digitalocean-credentials /var/creds.ini --non-interactive -m baktybek.uulu.shaiyrbek@gmail.com -d shaiyrbek.net.kg"

